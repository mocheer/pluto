# ec_md5

## 不安全

MD5算法存在安全问题，并且不再被官方推荐使用。如果在生产环境中使用哈希算法，请考虑更加安全的选项，如SHA-256。

- MD5算法的输出长度为128位，这意味着只有2^128个可能的哈希值，容易产生碰撞，不足够安全。
- MD5算法的设计特性，使其易受暴力破解和彩虹表等预计算攻击的影响。预计算攻击是指使用预先计算的哈希值和对应的输入，来推导出特定输入的过程。这使得攻击者可以通过对已知哈希值的预先计算和存储，快速恢复原始输入的可能值